name: artos-run
version: '0.1' # just for humans. Semantic versioning is recommended
summary: sdk for all run deps of ArtOS in Qemu # 79 char long summary
description: |
  installs qemu packages and debug tooling (gdb)
license: GPL-3.0 # your SDK's SPDX license
platforms: # The platforms this SDK should be built on and run on
  ubuntu@24.04:amd64:
parts:
  my-part:
    plugin: nil
plugs:
  desktop:
    interface: desktop